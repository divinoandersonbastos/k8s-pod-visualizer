# ─────────────────────────────────────────────────────────────────────────────
# K8s Pod Visualizer — On-Premises: NFS — Deployment
#
# Ordem de aplicação:
#   kubectl apply -f deploy/base/00-namespace-rbac.yaml
#   kubectl apply -f deploy/onpremises/nfs/00-pv-pvc.yaml
#   kubectl apply -f deploy/onpremises/nfs/01-deployment.yaml
# ─────────────────────────────────────────────────────────────────────────────

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-pod-visualizer
  namespace: k8s-pod-visualizer
  labels:
    app.kubernetes.io/name: k8s-pod-visualizer
    app.kubernetes.io/version: "1.3.2"
    deploy.environment: "onpremises-nfs"
spec:
  # Com NFS (ReadWriteMany), é possível rodar 2 réplicas para alta disponibilidade.
  # Para isso, certifique-se de que o SQLite está configurado com WAL mode.
  # Recomendado: manter replicas: 1 para evitar conflitos de escrita no SQLite.
  replicas: 1
  selector:
    matchLabels:
      app: k8s-pod-visualizer
  template:
    metadata:
      labels:
        app: k8s-pod-visualizer
        app.kubernetes.io/name: k8s-pod-visualizer
    spec:
      serviceAccountName: k8s-pod-visualizer
      # Sem nodeSelector: o pod pode ser agendado em qualquer node do cluster
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: k8s-pod-visualizer
          image: ghcr.io/divinoandersonbastos/k8s-pod-visualizer:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: PORT
              value: "3000"
            - name: NODE_ENV
              value: "production"
            - name: K8S_API_URL
              value: "https://kubernetes.default.svc"
            - name: USE_SERVICE_ACCOUNT
              value: "true"
            - name: DATA_DIR
              value: "/app/data"
          volumeMounts:
            - name: visualizer-data
              mountPath: /app/data
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/pods
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: visualizer-data
          persistentVolumeClaim:
            claimName: k8s-visualizer-data
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: k8s-pod-visualizer
  namespace: k8s-pod-visualizer
  labels:
    app.kubernetes.io/name: k8s-pod-visualizer
spec:
  selector:
    app: k8s-pod-visualizer
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
      nodePort: 30080
  type: NodePort
